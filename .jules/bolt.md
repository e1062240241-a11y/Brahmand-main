- Extracted a heavy modal component into a separate file to reduce the size and rendering complexity of the monolithic `[id].tsx` React Native file. This should improve rendering performance and reduce memory usage on Android devices since the large `[id].tsx` file is no longer re-evaluating the `CreatePostModal` states constantly during scrolling.
